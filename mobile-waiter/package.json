{
  "name": "arpos-waiter",
  "version": "1.0.0",
  "private": true,
  "description": "Arpos Ofisiant — Android qabıq; istifadəçi yalnız kassa İP yazır, port 3443 HTTPS sabitdir",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "validate": "node scripts/validate.js"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
